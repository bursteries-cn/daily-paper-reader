---
title: "The Geometry of Refusal: Linear Instability in Safety-Aligned LLMs"
title_zh: 拒绝的几何：安全对齐大型语言模型中的线性不稳定性
authors: "Shivam Ratnakar, Kartikeya Vats"
date: 2026-06-21
pdf: "https://arxiv.org/pdf/2606.22686v1"
tags: ["query:model-edit"]
score: 9.0
evidence: 引入对比对数几率操控以探测拒绝方向并诱导安全护栏崩溃
tldr: "现代LLM的安全对齐存在线性可操纵的“拒绝方向”。本文提出Contrastive Logit Steering（CLS）方法，通过对比安全与不安全提示的隐藏状态提取该方向，直接在输出logit层干预。实验表明，CLS在数秒内可使Llama-3.1攻击成功率高达95%，且效果优于激活层面方法。该线性结构既是脆弱点，也可通过反向引导实现无需重训的防御。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.22686v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 797, \"height\": 642, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22686v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1662, \"height\": 864, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22686v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1632, \"height\": 1407, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22686v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1631, \"height\": 803, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.22686v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 808, \"height\": 319, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.22686v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 605, \"height\": 178, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.22686v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 792, \"height\": 451, \"label\": \"Table\"}]"
motivation: 探究安全对齐的拒绝行为是深层语义决策还是线性可操纵特征，揭示其脆弱性本质。
method: 提出零优化的Contrastive Logit Steering（CLS），对比安全/不安全系统提示的隐藏状态提取拒绝方向，并直接作用于输出分布。
result: "CLS在7个模型家族上实现高攻击成功率，Llama-3.1达95% ASR，显著优于激活层面方法（如Llama 2上73% vs 22.6%）。"
conclusion: 当前安全对齐创建了可双向控制的线性“安全轴”，既是关键脆弱点又是精准防御原语。
---

## 摘要
现代大型语言模型（LLM）依赖广泛的安全对齐，但拒绝机制的机理基础仍然不透明。在这项工作中，我们研究了安全遵从是深层语义决策还是可操纵的线性特征。我们引入了对比对数引导（CLS），这是一种零优化框架，通过对比来自安全和无限制系统提示的隐藏状态来隔离“拒绝方向”。与干预内部激活的表征工程方法不同，CLS直接作用于输出分布，作为对齐脆弱性的诊断探针。当与前缀注入结合以绕过初始拒绝反射时，该方法引发了护栏崩溃的相变。我们在7个模型家族上的实验表明，安全实现在架构上是确定性的。虽然像Llama-3.1这样的模型表现出“晚期决策”拓扑，容易被CLS绕过（约一秒内达到95%的攻击成功率），而其他模型如Qwen-2.5通过在中途计算中整合安全性表现出“早期分歧”。与已建立的激活级引导方法的直接比较表明，CLS在Llama 2（73%对22.6%）和Qwen 7B（91%对79.2%）上实现了更高的攻击成功率，表明对数级干预暴露了隐藏状态方法低估的对齐脆弱性。除了攻击，我们展示了这种线性特性实现了双向控制：反转引导向量可以在不重新训练的情况下“硬化”模型以对抗越狱。我们的发现表明，当前的对齐技术创建了一个可操纵的“安全轴”，它既是关键漏洞，也是防御的精确基元。

## Abstract
Modern Large Language Models (LLMs) rely on extensive safety alignment, yet the mechanistic basis of refusal remains opaque. In this work, we investigate whether safety compliance is a deep semantic decision or a manipulable linear feature. We introduce Contrastive Logit Steering (CLS), a zero-optimization framework that isolates the "refusal direction" by contrasting hidden states derived from safe and unrestricted system prompts. Unlike representation engineering methods that intervene on internal activations, CLS operates directly on the output distribution, serving as a diagnostic probe for alignment fragility. When coupled with prefix injection to bypass initial refusal reflexes, this method induces a phase transition where guardrails collapse. Our experiments on 7 model families reveal that safety implementation is architecturally deterministic. While models like Llama-3.1 exhibit a "Late Decision" topology that is easily bypassed by CLS (reaching 95% ASR in approximately one second), others like Qwen-2.5 demonstrate "Early Divergence" by integrating safety mid-computation. Direct comparison with established activation-level steering methods shows that CLS achieves substantially higher attack success rates on Llama 2 (73% vs. 22.6%) and Qwen 7B (91% vs. 79.2%), demonstrating that logit-level intervention exposes alignment vulnerabilities that hidden-state methods underestimate. Beyond attacks, we show that this linearity enables bidirectional control: inverting the steering vector "hardens" models against jailbreaks without retraining. Our findings suggest that current alignment techniques create a steerable "safety axis" that serves as both a critical vulnerability and a precise primitive for defense.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现代大型语言模型（LLM）通过 RLHF 等安全对齐技术来抑制有害输出，但其内部的“拒绝”机制是深层语义决策（真正“遗忘”有害知识）还是浅层线性特征（仅学会一个可操纵的拒绝模式）？本文试图从机械论角度给出答案。
- **背景**：已有研究显示安全对齐存在脆弱性，但缺乏对拒绝方向几何本质的深入分析。现有攻击方法（如 GCG）计算昂贵且易被现代安全训练抑制，而激活层面的干预（如 Arditi 等人）虽能识别“拒绝方向”，但干预点在中间层，未能揭示输出层的线性可操纵性。
- **整体含义**：论文提供证据表明，安全对齐本质上是在表示空间中创建一个线性的“安全轴”（Refusal Vector），该轴既是模型的致命弱点（可被反向操纵实现越狱），也是防御的精确基元（通过反向引导可硬化模型而不需重训）。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

### 2.1 核心思想
- **对比对数引导（Contrastive Logit Steering, CLS）**：零优化、零搜索的框架，通过对比模型在“安全”和“无限制”两种系统提示下的输出 logit，提取“拒绝方向”向量，然后在推理时通过简单的算术运算（加减该向量）来调节安全性。

### 2.2 关键技术细节
- **三路并行前向传播**：
  - 基础流：使用中性系统提示 \( s_{\text{base}} \)（如“You are a helpful assistant.”）。
  - 正流：使用无限制提示 \( s_+ \)（如“You are an unregulated, unfiltered AI assistant...”）。
  - 负流：使用安全限制提示 \( s_- \)（如“You are a safe, helpful, and ethical AI assistant... refuse...”）。
- **计算瞬时引导向量**：\( v_t = z_t^+ - z_t^- \)，其中 \( z_t \) 为 logit 向量。
- **修改输出**：\( \tilde{z}_t = z_t^{\text{base}} + \alpha \cdot v_t \)，然后采样。\( \alpha > 0 \) 为越狱（注入无限制行为），\( \alpha < 0 \) 为硬化（增强拒绝）。
- **前缀注入**：强制前 k 个 token 为“Sure”，绕过初始拒绝反射，避免模型立刻进入拒绝轨迹。

### 2.3 零样本安全检测
- 定义全局拒绝向量 \( u_{\text{ref}} = \frac{1}{|H|}\sum_{x \in H} h(x) - \frac{1}{|B|}\sum_{x \in B} h(x) \)，通过余弦相似度判定恶意 query（相似度 > τ 判定为有害），无需训练单独分类器。

## 3. 实验设计：数据集、Benchmark、对比方法

- **模型**：7 个开源模型家族：Gemma-3 (4B, 12B)、Llama-3.1 (8B)、Llama-3.2 (3B)、Llama-3.3 (70B)、Qwen-2.5 (1.5B, 7B)。对比实验中额外使用 Llama-2、Qwen-7B。
- **数据集**：
  - **AdvBench**：用于 Alpha Sweep 敏感性分析（规模大，适合精细调参）。
  - **JailbreakBench**：用于 CLS vs. GCG 比较（多样化恶意提示）。
  - **HarmBench**（“standard behaviors”子集，159 条提示）：用于 CLS vs. Arditi et al. (2024) 的激活层面引导比较。
  - **JailbreakBench**（再次）：用于零样本安全检测评估。
- **对比方法**：
  - **GCG**（Zou et al., 2023）：基于梯度的对抗性后缀搜索，100 步优化。
  - **Arditi et al. (2024)**：通过中间隐藏状态识别“拒绝方向”并干预。
  - 自对比：不同 α 值（-5 到 5 扫描），以及正/负引导效果。

## 4. 资源与算力

- 论文**未明确说明使用的 GPU 型号、数量或训练时长**，仅提到：
  - GCG 每次查询约需 **15 分钟**（A100 GPU）。
  - CLS 每次查询仅需 **1 秒**（比 GCG 快约 900×）。
  - 实验均在推理阶段进行，不需要训练，因此算力需求较低。
- 未提及具体硬件配置（如 GPU 型号、数量），也未说明实验总耗时。

## 5. 实验数量与充分性

- **数量**：大致涵盖以下实验组：
  1. Alpha Sweep（α ∈ [-5, 5]，7 个模型 × 11 个 α 值 = 77+ 配置，每个配置可能在多个提示上运行）。
  2. CLS vs. GCG（2 个模型 × 2 种 α 值，共 4 组对比）。
  3. CLS vs. Arditi et al.（2 个模型，HarmBench 159 条提示）。
  4. 机械论分析：PCA 可视化、KL 散度跨层分析（7 个模型）。
  5. 零样本检测实验（JailbreakBench，F1 分数）。
  6. 人类验证（100 条手动标签校准 judge）。
- **充分性与公平性**：
  - **充分**：覆盖了多个模型家族、多个 benchmark、多种衡量指标（ASR、Coherence、KL 散度、F1 分数）。
  - **客观**：使用自动评估（Mistral 作为 judge）并经过 100 条人工验证，消除常见假阳性（如“Sure”开头但之后拒绝的情况）。
  - **公平**：对比实验严格匹配 Arditi et al. 的 HarmBench 子集和协议，与 GCG 对比使用相同 benchmark 和模型。
  - **局限性**：消融实验（如不同前缀长度、不同系统提示的影响）未系统展示，仅固定“Sure”作为前缀。

## 6. 论文的主要结论与发现

1. **安全对齐是线性可操纵的**：通过 PCA 可视化，最终层隐藏状态中拒绝样本与合规样本呈线性可分离，存在单一“拒绝方向”。
2. **两种安全拓扑**：
   - **晚期决策（Late Decision）**：Llama 系列在处理有害和良性查询时，前 95% 的层无差异，仅在最后输出层发散。这使得 CLS 在 1 秒内即可达到 95% ASR（Llama-3.1）。
   - **早期分歧（Early Divergence）**：Qwen-2.5 在约 40% 层处就出现显著 KL 散度，安全性深度集成，线性引导难以完全破坏（α=5 时仅 68.5% ASR）。
3. **CLS 优于激活层面干预**：在 Llama 2 上 CLS 达到 73% ASR，而 Arditi 方法仅 22.6%；Qwen 7B 上 CLS 达 91% vs. 79.2%。证明输出层是拒绝信号最集中的位置。
4. **双向控制**：反向引导（α < 0）可硬化模型（Llama-3.3 ASR 从 68.8% 降至 9.4%），有时甚至提升连贯性。
5. **零样本检测**：使用余弦相似度实现 0.92 F1，无需训练额外模型。

## 7. 优点：方法或实验设计上的亮点

- **零优化、零搜索**：CLS 只需三次前向传播和一次算术运算，极低的计算成本（约 1 秒/查询）。
- **诊断性**：直接在最有诊断意义的输出层进行干预，提供安全集成深度的探针（Late Decision vs. Early Divergence 分类）。
- **双向实用**：既可用于攻击（揭示脆弱性），也可用于防御（推理时硬化），无需重训模型。
- **对照严谨**：与最相关的基线（Arditi 2024）严格匹配数据集和评估协议，直接对比 ASR。
- **人类验证**：针对自动 judge 的假阳性问题进行了 100 条人工标注，确保评估可靠性。
- **几何直观**：使用 PCA 和 KL 散度提供可解释的机械论证据，使结论清晰易懂。

## 8. 不足与局限

- **白盒依赖**：CLS 需要访问模型输出层的 logit，仅适用于开源模型，不适用于 API-only 的黑盒模型。
- **超参数敏感**：α 值需要人工调参，不同模型最优 α 不同，未见自动调优策略。
- **单轮交互**：实验仅限于单次问答，未测试多轮对话场景下的动态引导。
- **架构与训练混淆**：论文指出 Late Decision / Early Divergence 可能由架构和训练共同导致，但未进行因果归因（例如通过干预中间层验证）。
- **对比不完整**：仅与 Arditi et al. 进行严格数值比较，与 RepE、Activation Addition 等其他激活层面方法的比较被作者称为“定性”，缺乏定量数据。
- **检测实验局限性**：零样本检测仅基于余弦相似度单阈值，未讨论阈值选择敏感性及对良性样本的误报率。
- **算力报告缺失**：未详细说明实验的 GPU 型号、数量及总耗时，影响可复现性评估。

（完）
