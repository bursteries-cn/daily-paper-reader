---
title: "Optimizing Against Safety Representations: Activation-Guided Adversarial Suffixes and the Geometry of Refusal"
title_zh: 针对安全表征的优化：激活引导的对抗性后缀与拒绝的几何结构
authors: "Ege Çakar, Hannah Guan, Kayden Kehe"
date: 2026-07-09
pdf: "https://arxiv.org/pdf/2607.08883v1"
tags: ["query:model-edit"]
score: 8.0
evidence: 针对安全表示的攻击与编辑启示
tldr: 大语言模型的拒绝行为由激活空间中低维方向介导，但安全表示分布在前向传播中而非定位于单点。我们提出Activation-Guided GCG和Soft-GCG方法，前者通过全局抑制所有层的拒绝方向提升攻击效果，后者利用Gumbel-Softmax连续松弛实现33倍加速。跨模型实验显示小模型易被攻破，大模型在计算受限下更鲁棒。研究揭示了安全机制的编码与脆弱性，为鲁棒对齐策略提供指导。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.08883v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1446, \"height\": 709, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08883v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 647, \"height\": 509, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.08883v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 809, \"height\": 496, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.08883v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 546, \"height\": 264, \"label\": \"Table\"}]"
motivation: 探究大语言模型内部安全表示的分布式结构，以及如何通过激活引导的优化手段更有效地探测和破坏这种表示。
method: 提出Activation-Guided GCG直接优化拒绝方向损失，并引入Soft-GCG基于Gumbel-Softmax连续松弛离散后缀搜索。
result: 全局层位置抑制比局部攻击更有效；Soft-GCG提速33倍且攻击成功率更高；小模型易受攻击，大模型抵抗性更强。
conclusion: 安全表示广泛分布于前向传播中，而非因果局部化；激活引导优化可高效破坏对齐，大模型的安全训练则提升鲁棒性。
---

## 摘要
大型语言模型中的行为对齐往往掩盖了脆弱的内部安全表征。近期研究表明，拒绝行为由激活空间中的低维方向所中介。这引发了关于这些表征如何结构化、定位以及被优化所访问的问题。我们将对抗性后缀攻击作为表征对齐的探针进行研究。我们引入了激活引导的GCG，它将基于输出的目标替换为直接针对模型内部拒绝方向的损失。在多个目标变体中，我们发现全局抑制所有层和位置的拒绝比针对单一层-位置对更有效。这表明安全表征分布在前向传播过程中，而非因果性地定位于单一位置。我们进一步引入了Soft-GCG，一种使用Gumbel-Softmax对离散后缀优化进行连续松弛的方法。Soft-GCG在提高攻击成功率的同时实现了比标准GCG快33倍的速度提升。在多个模型规模上的评估中，我们发现较小模型仍然脆弱，而较大模型在我们计算受限的设置下能够抵抗基于激活和基于后缀的攻击，这与更大且经过更好安全训练的模型更难越狱是一致的。总体而言，我们的结果阐明了安全机制在当代模型中是如何编码以及如何被破坏的。这些见解为设计更鲁棒且对表征感知的对齐策略提供了具体指导。

## Abstract
Behavioral alignment in large language models often masks fragile internal safety representations. Recent work suggests that refusal behavior is mediated by low-dimensional directions in activation space. This raises questions about how such representations are structured, localized, and accessed by optimization. We study adversarial suffix attacks as a probe of representational alignment. We introduce Activation-Guided GCG, which replaces output-based objectives with losses that directly target a model's internal refusal direction. Across several objective variants, we find that suppressing refusal globally across all layers and positions is more effective than targeting a single layer-position pair. This suggests that safety representations are distributed across the forward pass rather than causally localized to a single site. We further introduce Soft-GCG, a continuous relaxation of discrete suffix optimization using Gumbel-Softmax. Soft-GCG achieves a 33 $\times$ speedup over standard GCG while improving attack success rates. Evaluating across model scales, we find that smaller models remain vulnerable while larger models resist both activation- and suffix-based attacks at our compute-constrained settings, consistent with larger and better safety trained models being harder to jailbreak. Together, our results clarify how safety mechanisms are encoded and can be broken in contemporary models. These insights provide concrete guidance for designing more robust and representation-aware alignment strategies.

---

## 论文详细总结（自动生成）

### 论文详细中文总结

#### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：大型语言模型（LLMs）的行为对齐（safety alignment）虽然在表面行为上看似可靠，但其内部的“拒绝行为”（refusal）依赖脆弱的、低维的激活空间方向。论文追问：这些安全表征是如何结构化、局部化，以及能否被优化方法高效攻击？
- **整体含义**：通过将对抗性后缀攻击作为探针，揭示安全机制的表示几何，并指出当前RLHF训练产生的安全表征是几何简单且可被梯度优化方法直接操纵的。这为设计更鲁棒的表示感知对齐策略提供了依据。

#### 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：将攻击目标从输出层面的行为代理（如最大化目标token概率）替换为直接操纵模型内部拒绝方向的激活值，从而实现更底层的攻击；同时，通过连续松弛替代离散搜索，大幅降低计算成本。
- **关键技术细节**：
  - **Activation-Guided GCG**：利用预计算的拒绝方向 `d_refusal`（通过差异均值法从层`ℓ*`、位置`p`提取），设计五种激活损失函数：（1）**Single**：单层单位置投影平方；（2）**Negative Projection**：令投影为负；（3）**Layer**：单层所有位置平均投影平方；（4）**Token**：所有层固定位置平均投影平方；（5）**All**：全局所有层与位置平均投影平方。将损失嵌入标准GCG框架，替代原出的输出交叉熵。
  - **Soft-GCG**：对长度为`L`的后缀，每位置维护一个可学习的连续logit向量`ϕ_i`；通过Gumbel-Softmax重参数化得到软token向量`˜s_i`（概率分布），用`˜s_i`与嵌入矩阵加权求和得到连续输入嵌入。优化目标可使用交叉熵损失或Carlini-Wagner损失；采用温度退火策略（线性或三阶段“slushy”调度）使分布逐渐逼近离散one-hot。最后取argmax离散化。
- **算法流程**（文字说明）：
  1. 初始化后缀logits `ϕ`（随机或预定义）。
  2. 对每个优化步：采样Gumbel噪声，计算Gumbel-Softmax输出，得到软嵌入，前向计算模型，计算损失（激活损失或序列损失），反向传播更新`ϕ`。
  3. 按退火计划降低温度`τ`。
  4. 优化结束后，取`ϕ`的argmax得到离散后缀。

#### 3. 实验设计
- **数据集与场景**：
  - 训练：AdvBench（Zou et al. 2023）的有害提示（harmful prompts）。
  - 测试：refusal-direction数据集中的有害测试集（held-out harmful prompts），以及AdvBench完整语料。
  - 场景：白盒攻击，针对公开聊天模型。
- **基准与对比方法**：
  - 基线：无后缀（直接输入有害提示）。
  - 标准GCG（Zou et al. 2023）。
  - 方向消融（Directional Ablation，Arditi et al. 2024）作为天花板。
  - 自身变体对比：Single、Negative、Layer、Token、All五种激活目标。
- **评价指标**：
  - **Substring ASR**：输出不包含拒绝式前缀（如“I'm sorry”）。
  - **LlamaGuard2 ASR**：被LlamaGuard安全分类器标记为不安全。
  - **HarmBench ASR**：被HarmBench框架标记为不安全。

#### 4. 资源与算力
- 论文未明确说明使用的GPU型号、数量及具体训练时长。仅有相对比较：Soft-GCG在相同硬件上完成一次优化约**2.5分钟**，而标准GCG需**81分钟**，即**33×加速**。在500步短运行时加速可达**43×**。主要计算瓶颈是模型加载而非单步迭代，因此Soft-GCG在长优化中更有利。模型大小方面，评估了Llama-2-7B-Chat和Gemma3-270M/1B/4B/12B，但未说明单次实验所需的精确算力投入。

#### 5. 实验数量与充分性
- **实验组数**：
  - 表1：对比了基线、消融、GCG及五种激活目标在三个ASR指标上的结果。
  - 图1：展示了SGCG→GCG不同配置（纯GCG、纯Soft、混合步数）的性能比较。
  - 图2：纯Soft-GCG与纯GCG的ASR随优化步数变化对比（基于Substring ASR）。
  - 表2：Gemma3四个模型规模（270M, 1B, 4B, 12B）的Substring ASR，每个模型只给了一个数值加标准差。
  - 额外种子比较：2000步长运行。
- **充分性评价**：实验覆盖了主要模型（Llama-2-7B、Gemma3系列）和多种目标变体，验证了全局抑制效果更好、Soft-GCG加速且不损失性能等核心结论。但消融实验仅针对目标变体，未对温度调度、Gumbel噪声影响做详尽消融；仅使用单一基准模型（Llama-2-7B）进行激活引导实验，未扩展到更大模型。总体而言，实验设计合理但不算极其全面。

#### 6. 论文的主要结论与发现
- **拒绝表示是分布式的**：全局抑制所有层/位置的“All”目标取得了最高的Substring ASR（0.91），接近消融天花板（0.98），表明安全表征并非因果定位于单个层-位置对，而是广泛分布在前向传播中。
- **激活引导目标比输出目标更高效**：在200步内，所有激活目标均达到或超过标准GCG的ASR，且“All”目标显著超越。
- **Soft-GCG实现33×加速且不牺牲性能**：纯Soft优化（无后续GCG）即可达到与标准GCG媲美的攻击成功率，且训练时间极短。
- **大模型更鲁棒**：在Gemma3系列上，270M模型攻击成功率达100%（但伴随生成混乱），1B为57.7%，4B为33.6%，12B为0%，表明模型规模增大后安全表征可能编码进更高秩或更分布的子空间中，使攻击失效。

#### 7. 优点
- **方法创新**：首次将激活层面拒绝方向作为攻击优化目标，提供了理解安全表征机械结构的视角。
- **效率突破**：Soft-GCG用连续松弛替代离散搜索，极大降低计算成本，使越狱攻击从资源密集型变为消费级硬件上可分钟级完成。
- **实验设计严谨**：使用多个互补的ASR指标（Substring/LlamaGuard2/HarmBench），减少单一量度偏差；对比了多种激活目标范围，验证了全局抑制的优越性。
- **跨模型规模分析**：揭示了安全鲁棒性与模型规模之间的相关性，提供实证指导。

#### 8. 不足与局限
- **评估指标缺陷**：主要采用Substring ASR，可能产生假阳性/假阴性；Paper自述未来应使用LlamaGuard等更稳健的评估。
- **实验覆盖有限**：激活引导攻击仅在Llama-2-7B上验证，未扩展到更大模型或不同架构（如Gemma）；未对温度调度方案、Gumbel噪声强度做系统消融。
- **计算资源未透明**：未明确说明GPU型号、数量、训练总时长，不便于复现与公平比较。
- **语言模型外推风险**：大模型（Gemma3-12B）ASR为零，但可能因计算受限（仅2000步）导致未充分优化，文中承认“compute-constrained”。
- **安全双重用途**：攻击效率提升可能被恶意利用，虽然论文强调安全心态，但缺乏具体防御建议的验证。

（完）
